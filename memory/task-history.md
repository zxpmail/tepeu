# Task History

| Date | Session | Task | Key Decisions |
|------|---------|------|---------------|
| 2026-07-11 | terminal-fix | **修终端一字一命令 / 无法执行** | 前端按行缓冲+本地回显；后端整行写 GBK stdin；AI CLI 加中文映射与直通命令；bootRun 重启验证 echo/dir PASS |
| 2026-07-10 | product-spec-builder | Product-Spec.md creation & validation | Spring Boot 4.0 + Spring AI 2.0 GA; SQLite only; Web UI (pi-web ref) |
| 2026-07-10 | dev-planner | DEV-PLAN.md generation | 4 phases for v0.1.0; Phase 1: skeleton + workspace MVP |
| 2026-07-10 | design-brief-builder | Design-Brief.md generation | Tone: 专业克制; Density: 适中; Theme: 跟随系统; Motion: 极少; Ref: pi-web |
| 2026-07-10 | dev-builder Phase 1 | Backend skeleton + Frontend skeleton + Workspace MVP | 27 Java files, 12 TS/TSX files; frontend build clean; Docker setup |
| 2026-07-11 | resume (handoff) | **Phase 1 后端编译修复 + 工具链/文档对齐** | 上一会话 Phase 1 后端实际未编译通过。修 pom（Spring AI starter→`spring-ai-starter-model-*`）、MemoryRepository 空 final 捕获、ProviderController 泛型推断；`mvn compile` green；DEV-PLAN/dev-map 修正 Gradle→Maven、Tailwind 3→4。功能验收+code-review 待做 |
| 2026-07-11 | functional-acceptance | **Phase 1 功能验收（bootRun + curl 全栈）** | 再修 2 运行期 bug：application.yml 去掉 SQLite URL `?mode=wal`（Windows 非法字符→SQLITE_CANTOPEN；WAL 已由 PRAGMA 设，冗余）；MemoryRepository Jackson 2→3（Boot 4 默认 Jackson 3 `tools.jackson`，Jackson 2 `com.fasterxml` 无自动 bean）。验收全过：workspace CRUD / memory create+search+get / 统一错误(400/404) / provider 元数据 / key 存取 / 前端 served。**发现 §7.4 API Key 加密未实现（明文存+原样回显）→ Phase 1 该项验收不达标**。code-review + 浏览器视觉验收 待做 |
| 2026-07-11 | §7.4-encryption | **实现 LLM API Key 加密存储（§7.4）** | 新增 `CryptoService`（AES-256-GCM，自动主密钥文件 `~/.tepeu/master.key`，`enc:v1:` 格式）；`LlmProviderService` 写入加密/读出解密（遗留明文 passthrough）；`ProviderController` 脱敏回显 `first3+••••+last4`；application.yml 加 `tepeu.security.master-key-file`；.gitignore 加 `*.key`/`.tepleu`。验证：新 key DB 直读为 `enc:v1:`（91B，长度校验吻合），GET 脱敏，遗留明文行 passthrough+脱敏。见 ADR-006。Phase1 §7.4 验收**达标** |
| 2026-07-11 | browser-visual-acceptance | **Phase 1 浏览器视觉验收（gstack）** | 多面板布局 ✅（标题栏+侧栏 nav+主区，DOM 实测）；主题切换 ✅（light↔dark，`--color-bg` `#fff`↔`#1a1a1a`、header/h1 实测变色）；文件浏览器 ✅（`GET /api/files/list` 列出 hello.txt/notes 正确渲染）。发现 **FileBrowserView 不在 mount 时自动 loadFiles**（useFileBrowser 无 useEffect），须点 `~` 面包屑才列文件——UX 小 bug 待 code-review。Phase 1 验收**全部达标**，仅剩 /code-review |
| 2026-07-11 | fix-filebrowser-autoload | **修 FileBrowserView mount 自动加载** | `FileBrowserView` 加 `useEffect(() => loadFiles('/'), [loadFiles])`（原 useFileBrowser 无 mount 触发）。frontend rebuild 通过（tsc OK，新 bundle `index-BkoO7NAw.js`）。gstack 验：开 Files 自动 `GET /api/files/list` 200 且渲染 `seed.txt`（无须点 `~`）。|
| 2026-07-11 | code-review + batch-fix | **Phase 1 code-review（Mode A 4 维）+ Must-fix/auto-fix 批量修复 + 重验** | code-review 出 3 critical（C1 上传穿越/C2 终端 WS RCE/C3 EventLoop 死代码）+ 多 major。修：C1（filename 校验+startsWith 复查）、C2（禁用终端 WS）、C3（删 eventLoop.ts + 退役 ADR-003）、M2（Memory DTO）、M3（移除 tags + cursor→created_at + hasMore）、M5（FK connection-init-sql）、M6（错误脱敏+400/404 handler）、M8/M9（api 类型化+uploadFile）、M10/M11/M12（FileBrowserView api.readFile+useWorkspace deps+remount guard）、M13（FileController 日志）、m1（/switch）、m2（虚线程）、m4（删除按钮 group-hover）、m5（Theme 类型）、ON DELETE CASCADE。延期：M1/M4/M3-tags/C2 终端/m3（DEV-PLAN carry-over）。未做：M7（crypto passthrough，Should-fix，ADR-006 文档化）。验证：mvn/tsc/build 0 + 烟雾测试全过（含 C1→400/C2→404/cascade 删除→200）。|
| 2026-07-11 | phase2-chat-core | **Phase 2 Agent 对话（SSE 流式）核心 — T1–T4 + 验证** | T1（implementer）ChatService + ChatModelFactory（程序化构建 Spring AI 2.0 ChatModel from DB 加密 key；javap 验证 2.0 API；Boot 4.0.7 无冲突）；T2（implementer）`POST /api/chat/stream` SseEmitter（token/final/error 事件 + 错误码映射）+ SessionService/message 表（FK cascade）+ AgentOrchestrator（history→stream，工具循环 TODO T2b）；T3 useChat（fetch-ReadableStream 解析 POST SSE）+ ChatView；T4 ProviderSettingsView + ⚙ nav。验证：mvn 0 + 30 测试过 + tsc/build 0 + SSE 错误事件实测（UNKNOWN_PROVIDER 正确 emit）+ gstack Chat/Provider 面板渲染。**剩 Phase 2**：T2b 工具循环、SessionListView、首次 key 引导、真实 key 端到端流式（需用户提供 key）。|
| 2026-07-11 | phase2-tools (T2b) | **Phase 2 Agent 工具循环（T2b）** | implementer：javap 验证 Spring AI 2.0 `@Tool`+`ToolCallbacks.from()`+`ChatClient.defaultTools()`+`.stream().chatResponse()→Flux<ChatResponse>`；**ChatClient 自动 tool loop**（字节码验证 `ToolCallingAdvisor`，无需手写 Plan→Tool→Observe→Loop）；新增 `agent/tool/FileTools`（list_files/read_file，复用 `basePath` 路径穿越防护 + 8KB 读上限，read-only）；`ChatService.streamWithTools` + `AgentOrchestrator` 改用 ChatClient。49 测试过（19 新 FileTools）。T2b `tool_call/tool_result` SSE 可视化 TODO（工具已可调用，前端暂不显中间步骤）。验证：mvn 0 + 49 测试 + 重启后 SSE 错误链路 intact。|
| 2026-07-11 | phase2-toolviz + session-ui | **Phase 2 工具可视化 + 会话切换 + 首次提示** | T2b-visual（implementer）：`ToolCallback` 装饰器 `ToolEventEmittingCallback` + `ToolEventEmitter`（forSse，线程安全 sendLock）→ `ChatController` 发 `tool_call`/`tool_result` SSE 事件（58 测试，9 新）；前端 `useChat` 重构消息模型（role user/assistant/tool，token/tool 事件按序插入）+ `ChatView` 渲染工具卡片（🔧 tool · call/result）+ 会话切换下拉（listSessions/loadSession）+ 首次 key tip。tsc/build 0。**Phase 2 仅剩真实 key 端到端验证**（需用户提供 key/ollama）。|
| 2026-07-11 | phase2-closure (no-cred) | **Phase 2 收尾（无凭证项）：testConnection 占位修复 + code-review + M1/M2/M3 修复** | 用户选"先修无凭证项"。独立验证 58 测试过 + tsc/build 0。发现 4 medium：M1 `ChatService.streamWithTools` 工具三重注册（defaultToolCallbacks + .tools + .toolCallbacks）→ 改为单次 per-request `.toolCallbacks(wrapped)`（注：Spring AI 2.0 两个 ToolCallback... API 均已 deprecated，装饰器模式无法用非 deprecated 的 .tools(Object)，记 carry-over）；M2 `ProviderSettingsView` 缺 Test 按钮 + footer 文案错 → 加 Test Connection 按钮（!maskedKey 时 disabled）+ 修文案；M3 `useChat.appendToken` 共享对象 mutation → 改不可变替换；M4 `LlmProviderService.testConnection` 占位 return true → 实迁至 `ChatService.testConnection`（build model + model.call("ping") + catch→false，避免与 ChatModelFactory 循环依赖），`ProviderController` 注入 ChatService。新增 `ChatServiceTest`（5 测试：4 testConnection + 1 streamWithTools 装配 smoke）。验证：**63 测试过**（+5）、tsc/build 0、bootRun 启动 4.6s 无 DI 环、`POST /api/provider/test/openai`→500 CONNECTION_FAILED（旧占位返 200）、chat stream error 链路 intact（UNKNOWN_PROVIDER SSE error）、gstack 验 Test 按钮渲染+disabled→enabled 切换+key 加密脱敏 sk-••••rify。Low findings（L1 readFile char/byte、L2 SseEmitter double-complete、L3 workspaceId 校验、L4/L5）仅文档化未改。**Phase 2 仅剩真实 LLM e2e**（机器离线，无法验 OpenAI 调用；需用户提供 key 或起 ollama）。dev DB 残留 fake openai key（sk-fake-ui-verify，disabled，无害，用户可覆盖）。无 git 仓库（handoff 旧称有 commit 不实）。后端运行中 PID 6060 :30141。|
| 2026-07-11 | dev-builder Phase 3 | **Phase 3 记忆 UI + 文件操作增强 + 终端** | 7 Tasks 全过。T1: Workspace 文件隔离(M4)—Workspace.root_path + FileController per-workspace basePath。T2: Memory tags 过滤 API。T3: FileVersionService + 历史/预览/恢复端点。T4: Terminal WS 安全启用。T5: MemoryView UI。T6: FileBrowser 增强(highlight.js/marked/版本/拖拽)。T7: TerminalView(xterm.js+AI CLI)。验证：70 测试过 + tsc 0 + build 2.69s。|
| 2026-07-11 | dev-builder Phase 4 | **Phase 4 集成与发布 v0.1.0** | T1: Dockerfile 多阶段构建更新(fix outDir 路径+tepeu.jar finalName+ZGC)。T2: .dockerignore。T3: docker-compose.yml(SQLite 持久化卷+master.key 卷+healthcheck)。T4: SetupWizard 首次启动引导(API key→workspace→welcome 三步)。T5: RELEASE_NOTES-v0.1.0.md。T6: 全面自检(70 测试过+tsc 0+build 2.98s+已知限制文档化)。验证：70 测试过 + tsc 0 + npm run build 2.98s。|
