# CONTEXT.md
# 项目进度快照（极简）

## 当前在做什么
已推送私有仓库 GitHub；本地 DB/密钥/工作区不入库。

## 上次停在哪
仓库：https://github.com/zxpmail/tepeu （private）。技能用 `/` 或 `@` 调用。

## 近期关键决定
- 不提交 `tepeu.db*`、`workspaces/`、`master.key`；源码无真实 API key 硬编码
- 测试仅保留 `sk-test` 等占位
