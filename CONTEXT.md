# CONTEXT.md

当前：**kernel + llm(fake) + Loop 答复路径 + 工具循环，不是 OS。** 规范 ADR-016。诚实度 [docs/agent-os-gap.md](docs/agent-os-gap.md)。

组件 6：session / policy / bus / llm / loop / compose。词汇：identity / syscall。harness：conformance。

下一刀：llm 真 HTTP（一族一刀），或预算硬门，或 maintenance。禁止再写第三份投影。禁止称骨架可演示。
