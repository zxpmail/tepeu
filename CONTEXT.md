# CONTEXT.md
# 项目进度快照（极简）

## 当前在做什么
技能改为对话中用 `/技能名` 或 `@技能名` 显式调用，不再「启用即全程注入」。

## 上次停在哪
后端按消息解析技能并注入；输入框 / 与 @ 菜单列出已安装技能；「常用」仅影响排序。

## 近期关键决定
- 调用方式对齐 ReqForge：`/dev-builder …`、`@bug-fixer …`
- 安装套件后需手动用斜杠/艾特调用；开关改名为常用
