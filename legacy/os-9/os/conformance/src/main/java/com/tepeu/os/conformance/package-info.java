/**
 * conformance 载体 — {@link com.tepeu.os.conformance.ConformanceCase} / Check / MutableClock。
 * 是 harness，不是 os 组件。载体进本 jar；各组件套件在各自 {@code src/test}，不进组件发行 jar。
 */
package com.tepeu.os.conformance;
