/**
 * loop 本机默认实现（压缩、DoomLoop、序列卫兵）。聚合口 {@link com.tepeu.os.loop.SessionLoop} 与完成门留在父包。
 * 一轮结局（{@code SessionLoop}）与真正 {@code replaceRange} 走 JDK {@code System.Logger}
 *（{@code component=loop}）。host 另记 CLI 面。不打 inbox / 模型正文。
 */
package com.tepeu.os.loop.local;
