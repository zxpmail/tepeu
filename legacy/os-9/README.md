# legacy/os-9 — develop 九模块标本

> 2026-09-06：停止在该实现上合并与修补。从零重写的规划见 [`docs/rewrite-0.md`](../../docs/rewrite-0.md)。  
> 对标 v1：`legacy/backend`、`legacy/frontend`。

**源码**：`os/`、`host/`（不含 `target/`）。只读。不在标本上修产品行为。

**结构摘录**：[`docs/archive/os-9/`](../../docs/archive/os-9/)。

对照构建：`mvn -f legacy/os-9/os/pom.xml test`。
