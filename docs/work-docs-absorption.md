# E:\work\docs → tepeu 吸收清单（仅有利）

> **地位**：切片规划输入，非对账轮、不开新裁决。需裁决项走 [`os-baseplate.md`](./os-baseplate.md) §8.5。  
> **姿态（用户钉死）**：**只吸收有利**——闸门与诚实纪律；功能故事 / 换栈 / 产业清单 / GNEX 验尸细节一律不进。  
> **来源**：`E:\work\docs`（尤以 `v3.0/运行时/` 裁剪与启发总表、SessionLoop 单栈结项）。对照日期 2026-08-18。  
> **规范真相**：ADR-016 + os-baseplate；本文不得覆盖红线。  
> **近亲**：[`legacy-absorption.md`](./legacy-absorption.md)（v1 行为规格）；[`gnex3-reference.md`](./gnex3-reference.md)（影子时间线，同「只吸取有利」）。

---

## 0. 吸收边界

| 吸 | 不吸 |
|----|------|
| 完成=证据、未知即未知、授权只收紧、SoR 三分、有界续跑、闸失败重评估、单栈、学习≠立项 | 多 Agent/Swarm 壳、插件元框架、MCP/A2A/OTel/WASI 换栈主线、LLM 评委当完成门、记忆产品栈、运营/认证整机、方舟九宫格 UI、「有 X 缺闸门」的补故事立项 |

**一句话**：有利的只有「闸门与诚实」；功能故事零吸收。

---

## 1. 纪律八条（直接用）

| ID | 纪律 | docs 裁剪别名 | 落到 tepeu |
|----|------|---------------|------------|
| D1 | **完成 = 证据写入**；生命周期结束 / SSE / Todo 勾完 ≠ 交付完成 | LX-1 / P0-a / MC-2 / LH-1 | ③ Loop：统一完成出口硬闸 |
| D2 | **未知即未知**；无样本、无真价、无真沙箱禁止报漂亮默认值 | CW-2 / A | `llm.*` + Metering |
| D3 | **授权只收紧**；高风险默认审批；hold 不可绕过 | CW-1 / E / MC-1 | Policy 规则集 + ApprovalStore 回放 |
| D4 | **状态 SoR 独立于聊天**；不新开第四真相域 | LX-2 | 已有三 store（entries / registers / ledger）——印证，勿扩 |
| D5 | **有界续跑**；无效转移则停 | LX-3 / PA-3 | ③ Loop maintenance 上限 + 卫兵熔断 |
| D6 | **闸失败后显式重评估** | I | ③ Loop / Team 失败岔路 |
| D7 | **单栈**；部署回滚可以，应用层双路径 / 永驻 feature flag 不要 | SessionLoop 单栈结项 | LlmProvider / Loop 选型：断言冲突拒黑盒旁路 |
| D8 | **学习 ≠ 立项**；外部材料只供裁剪进契约，不扩尺子 | 尺子客观边界 | 对账 / 吸收类文档默认姿态 |

**P0 别名**（`E:\work\docs` 原别名，本库仅溯源引用，勿自行扩展）：P0-a = 交付证据优先；P0-b = 记忆平面话术——[`agent-os-gap.md`](./agent-os-gap.md) §3 的「P0-b 保护」同源。

---

## 2. 按刀口吸收（有利才排）

| 序 | 刀 | 只吸这些 | 不顺带 |
|----|-----|----------|--------|
| **0** | **`llm.*` 断言切片**（下一刀） | D2；§6-6 `derive∘normalize==sent`；注册表确定性规范序；用量/价格未知→n/a；LlmProvider 与断言冲突则拒黑盒（D7） | Spring AI「先跑通再补断言」；协议换栈 |
| **1** | **③ Loop** | D1 完成证据门进契约；D5 有界续跑；DoomLoop→ask；终态/恢复分级进端口说明；D6 失败重评估 | 先无闸跑通 turn；Swarm/Team 故事插队 |
| **2** | **预算硬门进真实往返** | Metering 供数 + Policy 拦截；超限不得 claim/开跑 | 「端口有了=门有了」 |
| **3** | **审批规则集做真** | D3；默认矩阵（可对齐 legacy A1）；同 turn 可回放 | 抄方舟面板 |
| **4** | **记忆（更后）** | **无平面则产品话术闭嘴**（保护性吸收，非功能） | 向量/图/MemOS/自演化 |

---

## 3. 与已有资产对齐（避免假缺失复读）

| 已有 | 说明 |
|------|------|
| 第九轮 Policy / ApprovalStore / C2 fail-closed | D3 的**端口地板**已有；规则集与同 turn 回放仍属序 3，禁止当 E 已过 |
| 三 store + Metering 端口 | D4 已印证；持久化 barrier / 预算真实往返属序 2 |
| §6-6 红线 + §8.5 LlmProvider / 规范序 drift | 序 0 必须销；纸面红线 ≠ 已证伪 |
| `legacy-absorption` A1 / A2 | A1→序 3 规则种子；A2 写声称路径证据 → 与 D1 同族，Loop 完成门对齐 |

**距 OS**：五层最低条与成色债见 [`agent-os-gap.md`](./agent-os-gap.md)。

---

## 4. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-18 | 初版：自 `E:\work\docs` 严苛提炼后按「只吸收有利」收口；八纪律 + 按刀映射 |
