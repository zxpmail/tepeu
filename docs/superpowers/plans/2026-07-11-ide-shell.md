# IDE 三栏布局 Implementation Plan

> **For agentic workers:** 按任务顺序实现。

**Goal:** Tepeu 主界面改为 pi-web 风格三栏 IDE，并补齐对话用量/过程折叠/输入工具条。

**Architecture:** `App` 默认挂载 `IdeShell`；左栏会话+文件树；中栏复用 `ChatView`；右栏 `RightFilePanel`。记忆/终端/服务商/工作区管理仍为次级全屏面板。

**Tech Stack:** React + Vite + 现有 CSS 变量

## 文件
- 新建 `components/layout/IdeShell.tsx`
- 新建 `components/layout/SessionSidebar.tsx`
- 新建 `components/files/RightFilePanel.tsx`
- 改 `App.tsx`、`ChatView.tsx`、`ChatInput.tsx`、`MessageView.tsx`、`useChat.ts`、`index.css`

## Tasks
- [ ] IdeShell 三栏 + 折叠 CSS
- [ ] SessionSidebar 会话/文件/底栏
- [ ] RightFilePanel 预览
- [ ] ChatView 适配 + ProcessDetails
- [ ] MessageView usage + ChatInput 工具条
- [ ] useChat 把 usage 挂到助手消息
- [ ] build 验证
